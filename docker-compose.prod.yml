services:
  frontend:
    ports:
      - "127.0.0.1:8090:80"
    volumes:
      - ./nginx/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/.htpasswd:/etc/nginx/.htpasswd:ro

  backend:
    environment:
      TRAINER_DATABASE_URL: sqlite:////app/data/trainer.db
