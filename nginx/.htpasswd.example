# Сгенерируйте файл паролей:
# docker run --rm -it httpd:2.4-alpine htpasswd -nbB admin YOUR_PASSWORD > nginx/.htpasswd
#
# После реализации пользовательских аккаунтов удалите auth_basic из frontend/nginx.conf
